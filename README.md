# MAIN_ComputerVision

githublink: https://github.com/B00150149/MAIN_ComputerVision

had to .gitignore venv folder since its too big. These rae the steps. 
Create virtual environment (Venv) and install these inside
py -3.10 -m venv venv  (Make sure its python 10 tensorflow doesnt work on new python)
venv\Scripts\activate
pip install tensorflow 
pip install matplotlib 
pip install keras 
pip install keras-tuner 
pip install tensorboard      
pip install scikit-learn
pip install tf-explain
pip install opencv-python
